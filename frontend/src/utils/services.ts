const baseUrl = "https://api.letsth.ink";

export const RoomsService = `${baseUrl}`;
export const MessagesService = `${baseUrl}`;
export const PollsService = `${baseUrl}`;
export const ModeratorService = `${baseUrl}`;
export const EmailService = `${baseUrl}`;
export const UsersService = `${baseUrl}`;
export const AuthService = `${baseUrl}`;
export const ExpirationService = `${baseUrl}`;
export const SiteHealthService = `${baseUrl}`;
export const VisualizerService = `${baseUrl}`;
export const QueryService = `${baseUrl}`;
export const VoteService = `${baseUrl}`;
